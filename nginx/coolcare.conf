server {
    listen 80 default_server;
    server_name _;

    # Максимальный размер загружаемого файла
    client_max_body_size 20M;

    # Логи
    access_log /var/log/nginx/coolcare_access.log;
    error_log /var/log/nginx/coolcare_error.log;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Таймауты
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
    }
}

# Раскомментируйте блок ниже ПОСЛЕ установки SSL-сертификатов (Certbot / Самоподписанные)
# server {
#     listen 80;
#     server_name _;
#     return 301 https://$host$request_uri;
# }
# 
# server {
#     listen 443 ssl default_server;
#     server_name _; 
# 
#     # SSL Configuration
#     ssl_certificate /etc/nginx/ssl/coolcare.crt;
#     ssl_certificate_key /etc/nginx/ssl/coolcare.key;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
# 
#     client_max_body_size 20M;
#     access_log /var/log/nginx/coolcare_access.log;
#     error_log /var/log/nginx/coolcare_error.log;
# 
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300;
#         proxy_connect_timeout 300;
#         proxy_send_timeout 300;
#     }
# }
